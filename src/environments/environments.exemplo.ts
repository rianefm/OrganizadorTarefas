export const environment = {
    production: false,
    firebaseConfig: {
      apiKey: "YOUR_API_KEY_HERE", // Substitua por sua chave de API do Firebase
      authDomain: "your-project.firebaseapp.com", // Substitua pelo seu authDomain
      projectId: "your-project-id", // Substitua pelo ID do projeto
      storageBucket: "your-project.appspot.com", // Substitua pelo storageBucket
      messagingSenderId: "your-sender-id", // Substitua pelo senderId
      appId: "your-app-id", // Substitua pelo App ID
      measurementId: "your-measurement-id" // Substitua pelo measurementId (opcional)
    }
  };
  