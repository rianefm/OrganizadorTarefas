import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'organizador de tarefas ',
  webDir: 'www'
};

export default config;
